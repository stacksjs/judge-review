[build]
  base = "."
  command = "bun install && touch .env && bun --cwd storage/framework/core run build && ./buddy build views"
  publish = "storage/framework/views/web/dist"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-lighthouse" 